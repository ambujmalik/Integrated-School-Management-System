version: "3.8"
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: ismp
      POSTGRES_USER: ismp
      POSTGRES_PASSWORD: ismp
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  backend:
    build:
      context: ./ismp-backend
      dockerfile: Dockerfile
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/ismp
      SPRING_DATASOURCE_USERNAME: ismp
      SPRING_DATASOURCE_PASSWORD: ismp
    depends_on:
      - postgres
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ./ismp-frontend
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    depends_on:
      - backend

volumes:
  pgdata:
